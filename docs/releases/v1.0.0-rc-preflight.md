# JARVIS v1.0.0 Release-Candidate Preflight

**Run date:** 2026-08-25
**Repository:** `Jamie1304/jarvis`
**Branch:** `agent/v1-integration`
**Current base HEAD:** `3bee1a268d8e23bb9949c3de7a7bdee3887dee69`
**Working tree:** mutable post-RC remediation; intentionally uncommitted
**Canonical release Python:** local external validation environment (host path
intentionally omitted from the release record)
**Python:** 3.13.14

This is a mutable-working-tree preflight only. No commit, push, tag, merge,
publication, or release was performed. Exact-SHA CI is intentionally pending
the separately authorized freeze/push run.

This document is updated by later mutable remediation runs. `READY_TO_FREEZE`
is not an authorization to freeze a changed tree; a new preflight must be
generated after this run's uncommitted production-composition changes are
reviewed and tested.

## POST-RC-CI REMEDIATION

The previous immutable RC remains failed and is never treated as approved:

```text
RC_CI_FAILED
SHA: f63b225ec8117c4d4b5e07eff73271f9dfe62911
GitHub CI runs: 32835066972, 32835070462
Failure: quality / seven sandbox tests exited before their first IPC response
```

The failure was reproduced as a test-boundary defect: generic protocol/lifecycle
tests used the Windows `RESTRICTED_TOKEN` diagnostic launcher through their
shared helper. Those tests did not require restricted-token semantics, but
therefore depended on cross-version restricted Python bootstrap behavior. The
old RC had no startup diagnostics, so the native child-bootstrap subcause is
not claimed as proven from that historical run. The remediation moves generic
tests to explicit `JOB_OBJECT_ONLY` diagnostic mode, retains a focused
restricted-token test, and adds bounded `SandboxStartupDiagnostics` for early
child exit. The production executable contract remains capability-free
AppContainer plus scoped ACLs, explicit handles, and Job Object; restricted-token
and Job-only modes remain non-certifiable.

### SKIPS

The opt-in identity observation was enabled for the final local gate, producing
six skips. The exact skips were:

- `tests/test_artifacts.py::test_artifact_root_rejects_reparse_ancestor_when_supported`:
  symlink creation unavailable to this test process;
- `tests/test_improvement.py::test_symlink_escape_is_rejected_when_platform_supports_links`:
  symlink creation unavailable to this test process;
- `tests/test_permissions.py::test_symlink_or_junction_escape_is_denied_when_supported`:
  symlink/junction creation unavailable to this test user;
- two optional `tests/test_windows_integration.py` owned-desktop/process checks:
  `JARVIS_WINDOWS_INTEGRATION` was not enabled;
- one optional camera check in `tests/test_windows_integration.py`:
  `JARVIS_CAMERA_INTEGRATION` was not enabled.

These are `ACCEPTABLE_HOST_SPECIFIC` or `OPTIONAL_HARDWARE` skips. No
AppContainer, certification, activation, sandbox fail-closed, deterministic
workflow, deterministic permission, or v1 acceptance security test is skipped.
The restricted-token compatibility observation was executed; it is not hidden
by a skip.

The earlier seven-skip local run had one additional opt-in adoption skip:
`tests/test_adoption.py::test_real_windows_identity_is_opt_in_and_observation_only`.
It passed when `JARVIS_RUN_WINDOWS_IDENTITY_TESTS=1` was enabled for this final
preflight, so it is not part of the final six-skip count.

## Three previous blockers

The three blockers identified for the R3.5A12/R3.5A13/R3.5A14 follow-up were
checked against the current production code, deterministic acceptance path,
trusted-core tests, and the local Windows observation where applicable.

| Follow-up | Result | Evidence and boundary |
|---|---|---|
| R3.5A12 — adoption identity, signer, and dependency provenance | **RESOLVED** | `AdoptionIdentityInspector` binds handle-derived file identity, bounded content hash, reparse status, signer evidence, independent dependency provenance, scope, and expiry. `tests/test_adoption.py -k real_windows_identity_is_opt_in_and_observation_only`: 1 passed. Synthetic negative tests reject forged, stale, changed, reparse, incompatible, and unavailable evidence. Adoption evidence never grants authority. |
| R3.5A13 — production Compensation + Verification composition | **RESOLVED** | `test_v1_acceptance_production_compensation_verifies_and_traces` uses the runtime-owned `CompensationService`, canonical PlanningEngine/ToolRegistry/PermissionBroker execution, durable lifecycle metadata, restart/stale-state checks, and independent VerificationEngine evidence. It does not accept model or callback claims. |
| R3.5A14 — UI simulation evidence linked through PackageCertifier | **RESOLVED** | `UISimulationAttestation` is runtime-harness-issued, self-digested, and bound to package/version/source/UI-manifest hashes, semantic/security checks, ArtifactStore references, and zero real effects. `PackageCertifier` requires it for UI-bearing packages; activation and lifecycle restore retain only the trusted reference/digest. `test_v1_acceptance_ui_certification_is_bound_through_composed_activation` covers Shadow/Canary/Active and restart. |

## Previously resolved evidence

No regression was found in the prior release properties. The full quality gate,
deterministic permissions/workflows, v1 acceptance, and focused adoption,
compensation, UI-certification, lifecycle, recovery, browser, credential,
voice-boundary, sandbox, and trusted-core tests remain green. The current
composition still preserves:

- quality coverage at 90% or greater;
- canonical AppContainer generated-code containment with fail-closed weaker or
  unavailable posture;
- authenticated Recovery/LKG;
- scoped CredentialVault use and secret redaction;
- Trace correlation;
- durable Workflow/Procedure state;
- trusted Shadow/Canary effect attestations;
- non-authorizing privileged voice and microphone/authority separation;
- canonical Browser Broker and deny-first unavailable fallback;
- production capability restoration, provisioning/setup resume, lifecycle
  durability, OpportunityEngine, AttentionPolicy, UpdatePreview, and
  adopt-before-install;
- package-code/user-data separation.

## Windows evidence boundary

The authoritative matrix remains [`v1-windows-acceptance.md`](../v1-windows-acceptance.md).
This run does not promote `NOT_EXECUTED`, `NOT_AVAILABLE`, or `NOT_PROVEN` to
PASS. The explicit opt-in command was run with repository-safe environment
flags and passed all three owned Windows checks:

```text
JARVIS_CAMERA_INTEGRATION=true
JARVIS_WINDOWS_INTEGRATION is supplied by --allow-hardware
\.venv\Scripts\python.exe scripts/run_system_tests.py --suite windows-hardware-manual --allow-hardware
```

Result: **PASS — 3 passed**, run `3b60844d-eb09-437c-9ca5-7413d18f687d`.
The independent adoption identity observation also passed as recorded above.
The documented local evidence covers owned Notepad/UI automation, owned-process
identity/environment, camera lifecycle, AppContainer/ACL/Job/IPC containment,
and fail-closed unsupported posture. It does not claim universal same-user,
VM, kernel, or dedicated-account isolation.

Optional or incomplete paths remain bounded and safe: physical voice output and
full voice runtime, browser companion, configured MCP, external generated
package effects, backup/migration, signed self-update execution, and long-term
manual operations are not silently treated as PASS. Optional absence degrades
or remains unavailable without changing permission policy.

## Security status

The current local evidence identifies zero unresolved reachable release-blocking
CRITICAL findings and zero unresolved release-blocking HIGH findings without
effective containment on the declared v1 paths. Specifically:

- generated executable packages use the canonical AppContainer path;
- Recovery/LKG uses authenticated trusted authority;
- adoption requires independent identity/provenance evidence;
- Shadow/Canary promotion requires trusted broker effect attestation and
  independent verification;
- privileged voice approval remains disabled by design;
- browser effects use the canonical broker;
- credential use is scoped through the trusted CredentialBroker;
- UI-bearing certification requires trusted `UISimulationAttestation`;
- compensation uses normal authority and VerificationEngine;
- model, page, MCP, package, document, event, and UI content remains untrusted;
- no donor framework is a runtime or security authority.

Historical R2/R4 reports retain their original snapshots and limitations. Later
remediation evidence above is the current disposition; no historical NOT-GO
snapshot is silently rewritten as if it were this run.

## Architecture and state ownership

JARVIS remains a minimal adaptive core. No product-specific integration catalog,
donor runtime, website-specific browser path, product-specific trigger, or
fixture-specific production adapter was added. New capability acquisition still
follows `DISCOVER -> ADOPT -> REUSE -> BUILD`.

The authoritative state map has one owner for each current durable domain:

- adoption/setup decisions and `AdoptionAttestation`: `SQLiteSetupStore`;
- compensation lifecycle: `CompensationStore`/`CompensationService`;
- UI simulation/certification evidence: trusted `UISimulationAttestation`, with
  reference/digest in the capability lifecycle certification record;
- rendered UI artifacts: `ArtifactStore`, not certification state.

No competing writer or separate database is introduced by these remediations.

## R4 production capability-growth remediation

The previous gap was that production construction selected unavailable package
generation/runtime seams. The current mutable tree composes one generic path:

```text
ProviderRegistry -> ProviderRouter -> bounded AgentLoop
 -> AgentRuntimeCapabilityGenerator -> ProductionPackageStore
 -> PackageReviewer/PackageCertifier -> AppContainer sandbox
 -> SetupConductor/typed ProvisioningEngine
 -> PackageActivationService -> sealed ToolRegistry activation port
 -> GeneratedCapabilityToolAdapter -> PermissionBroker -> package runtime
 -> trusted Shadow/Canary attestation -> ACTIVE/hot-load -> VerificationEngine
```

The production acceptance test uses a randomized capability and a fake provider
registered through the normal registry; it does not use `RuntimeTestFixture`,
fixture-specific production adapters, direct ACTIVE writes, donor runtimes, or
product-specific branches. The test asserts that the randomized typed action is
selected by PlanningEngine, resolved by ToolRegistry, passed through
PermissionBroker, executed by the package runtime, schema-validated, and
accepted by VerificationEngine before the original goal completes. Package
bytes are hash-addressed and external to the lifecycle database;
lifecycle/certification/activation truth remains in
`SQLiteCapabilityLifecycleStore`. A missing routable model/provider fails
closed as `WAITING_FOR_MODEL_PROVIDER` rather than selecting an unavailable
generator.

The exact native production acceptance test and the complete post-change gate
results are recorded below. This remains a mutable preflight and does not
authorize freezing or release.

## Version and release metadata

- Product version: **1.0.0**, from `jarvis.version.__version__` and dynamic
  package metadata; no conflicting `VERSION.md` exists or is required.
- `CHANGELOG.md` and `docs/releases/v1.0.0.md` exist and reflect the current
  adoption evidence boundary.
- CI config includes quality, deterministic workflows, deterministic permissions,
  and v1 acceptance on push and pull request.

## Required local gates

The older rows in the first table below are retained as historical evidence. The
current post-change gate record follows it and uses the canonical `.venv`
executable on the current mutable tree.

| Gate | Result |
|---|---|
| `python scripts/quality.py` with synthetic `JARVIS_ENVIRONMENT=test` and opt-in identity check | **PASS** — 1,361 passed, 6 skipped; Ruff format/check and mypy passed; 90% combined coverage |
| `python scripts/run_system_tests.py --suite deterministic-workflows` | **PASS** — 26; run `77c1a42f-a77c-49af-9474-3bed76b3afc5` |
| `python scripts/run_system_tests.py --suite deterministic-permissions` | **PASS** — 72 passed, 1 skipped; run `45c99889-79f4-4601-9fbe-4cb698a3f729` |
| `python scripts/run_system_tests.py --suite v1-acceptance` | **PASS** — 21; run `0886cb96-53a9-4381-95d7-55150cfff72a` |
| seven historical CI sandbox scenarios | **PASS** — 7 passed, 16 deselected |
| focused sandbox/certification/lifecycle/acceptance set | **PASS** — 109 passed |
| `python scripts/run_system_tests.py --suite windows-hardware-manual --allow-hardware` with camera opt-in | **PASS** — 3; run `b8c2922b-52a1-4b98-bbc1-bd5534cdb7cb` |
| adoption identity observation | **PASS** — 1 passed, 13 deselected |
| `git diff --check` | **PASS** — line-ending normalization warnings only |

## Current post-change gate record

| Gate | Result |
|---|---|
| `python scripts/quality.py` with explicit `JARVIS_ENVIRONMENT=test` | **PASS** — 1,445 passed, 7 skipped; Ruff format/check, mypy, and 90% combined coverage |
| `python scripts/run_system_tests.py --suite deterministic-workflows` | **PASS** — 26 passed; run `dbbeea9d-35c2-4958-8a6b-ae682cd220b0` |
| `python scripts/run_system_tests.py --suite deterministic-permissions` | **PASS** — 72 passed, 1 skipped; run `7769f4d7-f085-4582-b828-8546c6bcc063` |
| `python scripts/run_system_tests.py --suite v1-acceptance` | **PASS** — 23 passed; run `e1d0395f-291e-4f72-81fa-5adca390a35b`; timeout 300 seconds |
| `git diff --check` | **PASS** — line-ending normalization warnings only |

The default local quality invocation was also attempted on 2026-08-25. This
host's Windows Credential Manager returned Win32 error 8 while creating the
secure recovery authority, so default-runtime tests fail closed with
`RecoveryAuthorityUnavailable`. No plaintext or implicit in-memory fallback
was enabled; the explicit test environment uses the repository's designated
`TestOnlyInMemorySecretBackend`.

## Package precheck

The canonical command `python -m pip wheel . --no-deps --no-build-isolation`
produced `jarvis-1.0.0-py3-none-any.whl` with 199 archive entries and SHA-256
`2c7a673f7488f34d92c8706eb9bf69f97b60e3dbc46c61a839ab20af5eab6e2`.
The content audit found only `jarvis/**` package code and standard dist-info;
no `.jarvis` state, local database, secret, credential file, recording, model
weight, generated integration, donor runtime, `tests/`, or `docs/` entry was
present. Mutable user data remains external to package code.

## Current R4F package precheck

The current mutable tree was rebuilt with the canonical release Python and
produced `jarvis-1.0.0-py3-none-any.whl` with 200 archive entries and SHA-256
`5542f37141c3628b30d9120b4815bd9c3ba8e22119aed56362b718d5b8ce23c`.
The content audit found only `jarvis/**` and standard `dist-info` entries;
there were no runtime databases, credentials, recordings, model weights,
donor runtimes, tests, docs, or generated user integrations.

## Current R4G semantic self-expansion preflight

The historical candidate `1860c37763c923db97349a437a83963412daab64` remains
permanently `R4_NO_GO`. The current mutable tree adds a separate randomized
production-composition acceptance proof; it does not rewrite that historical
result. The proof enters through `ApplicationRuntime.create()` and
`GoalSupervisor.start()`, establishes an initial semantic capability gap,
creates a typed generated action, runs package-specific certification with an
application-owned semantic oracle, activates through the trusted lifecycle and
ToolRegistry ports, executes through PermissionBroker, independently verifies
the result, and restores/reuses the exact package after restart. Assertions
require the generated action to be selected and invoked, so an unrelated
built-in tool cannot satisfy the challenge. Wrong semantic output fails
certification before ACTIVE.

The associated evidence is documented in
[`v1.0.0-r4g-production-composition-acceptance.md`](v1.0.0-r4g-production-composition-acceptance.md).

The R4G package precheck produced `jarvis-1.0.0-py3-none-any.whl` with 201
archive entries and SHA-256
`ec4c62e4c981b482e04b63b24f414514a6aa10dd2ae1a32d2ae9fce1b8835e96`.
The archive contains only `jarvis/**` and standard distribution metadata;
the content audit found no runtime state, credentials, local databases,
models, recordings, generated user integrations, donor runtimes, tests, or
documentation.

## Post-R4 opportunity lifecycle remediation

The immutable candidate `69a39ae5b438d09471fa5f9bda90ead98b8df78c` is permanently
`R4_NO_GO`. R4 reproduced an acquisition exception that persisted
`READY_TO_PROPOSE` with `preparation_state=FAILED`; the then-current proposal path
checked only the status. The earlier preflight claim that opportunity failures had
already been proven non-proposal-ready was incomplete for that candidate.

The current mutable follow-up corrects the transition, validates readiness in both
proposal and acceptance, reconciles impossible persisted rows, and adds restart and
failure-matrix tests. This preflight predates that follow-up and must not be used to
freeze it; a new preflight and exact-SHA CI run are required after the remediation.

## Working-tree audit

The working tree contains the production capability-growth implementation, its
deterministic tests, and the related architecture/acceptance/preflight
documentation. No runtime state, credentials, machine data, caches, recordings,
model weights, generated integrations, or temporary build output is in the
repository scope.
Git-ignored local test state (`.jarvis`, `.coverage`, `.pytest_cache`, and
`build`) is present outside the tracked diff; it is not release content and was
not staged or included in the candidate scope.

The complete mutable diff is classified as follows: production composition and
security implementation (`jarvis/agent_runtime.py`, `capability_acquisition.py`,
`capability_factory.py`, `credentials.py`, `effects.py`,
`environment_discovery.py`, `package_certification.py`, `runtime.py`,
`security/startup.py`, and `testing/catalog.py`); regression tests
(`tests/test_credentials.py`, `tests/test_effects.py`, and
`tests/test_v1_acceptance.py`); release/architecture documentation (the
modified `CHANGELOG.md` and files under `docs/`); and the following new,
release-scoped files: `jarvis/production_capability.py`,
`tests/test_production_capability.py`,
`docs/architecture/capability-lifecycle-restoration.md`,
`docs/architecture/production-capability-growth.md`, and
`docs/releases/v1.0.0-r4f-production-sweep.md`,
`jarvis/generated_capability.py`, and
`docs/releases/v1.0.0-r4g-production-composition-acceptance.md`. No
unexplained entry remains. The generated-capability module is the trusted
application adapter boundary for certified package actions; it is not a user
generated integration or donor runtime.

## Final decision for this preflight

The previous candidate remains permanently `R4_NO_GO`/`RC_CI_FAILED` and is not
being relabeled. The current mutable tree closes the four R4F blockers: usable
production acquisition, lifecycle restoration, production compensation
observation/verification, and release claims reconciled to actual evidence.
Production self-expansion is proven only for the documented generic BUILD path
with a routable provider, supported Windows sandbox, bounded resources, and
normal authority. Adoption of arbitrary external installations, first-run
wizard bootstrap, complete package-local repair playbooks, physical optional
hardware, and exact-SHA CI remain explicitly outside this mutable preflight's
claim; they are not silently promoted to PASS.

This historical preflight's required gates passed at 90% coverage or greater,
but it is superseded by the later OpportunityEngine lifecycle finding and the
current mutable remediation. No commit, push, tag, merge, publication, or release
was performed. The remediation must receive a fresh preflight and exact-SHA
GitHub CI before it can be called frozen or green.

SUPERSEDED_BY_MUTABLE_REMEDIATION

## Post-Run30 installed-distribution remediation

The immutable candidate `57862f6777e3dc1966f8f1c07688fde7257e7428` was
authorized by R4 for packaging, but Run 30 correctly returned
`PACKAGE_FAILED_REQUIRES_NEW_RC`: an artifact-only startup could not rely on a
developer checkout's generated project knowledge and source-layout evidence.
That candidate remains historical and is not publishable.

The follow-up remediation separates source-checkout integrity evidence from
installed-distribution evidence. Source/CI composition validates the actual
checkout files. Installed production composition validates package-local
trusted modules, distribution version, and their `RECORD` SHA-256/size entries;
it does not require `docs/`, `scripts/`, `tests/`, or repository
`pyproject.toml`, and it does not claim publisher authenticity without signing.
Missing or malformed installed evidence still fails closed. Missing optional
project knowledge becomes an empty projection, while present knowledge remains
strictly validated.

The remediation requires a new release candidate and fresh exact-SHA CI. It
does not amend, relabel, tag, publish, or release the historical candidate.

## Final mutable preflight after OpportunityEngine remediation

The current mutable tree contains only the narrow OpportunityEngine lifecycle
fix, focused regressions, and affected documentation. The historical SHA
`69a39ae5b438d09471fa5f9bda90ead98b8df78c` remains permanently `R4_NO_GO` and
is not reused.

The fix proves that acquisition exceptions persist `FAILED` and are not
proposal-ready; `proposal()` and `accept()` reject failed, blocked, unknown,
waiting, degraded, and stale states. Stores reject new impossible combinations,
and legacy `READY_TO_PROPOSE + FAILED` rows reconcile to non-ready state while
retaining evidence and diagnostic errors. Successful `READY` preparation still
supports proposal and normal coordinator acceptance.

Final mutable checks on 2026-08-26:

- focused OpportunityEngine regressions: PASS, 7 selected;
- full quality: PASS, 1,458 passed, 7 skipped, 90% coverage, Ruff and mypy pass;
- deterministic workflows: PASS, 26 passed, run
  `e0c4052c-74b2-4fb6-9d52-cda87bb33347`;
- deterministic permissions: PASS, 72 passed, 1 skipped, run
  `863b572f-ad4f-4184-9d35-89b996e4b359`;
- v1 acceptance: PASS, 23 passed, run
  `17a2615c-2ca1-4da9-8b86-bcbfb939c0bd`;
- package precheck: PASS, `jarvis-1.0.0-py3-none-any.whl`, 201 entries, SHA-256
  `34e9880db3e61432083b0b6ac0271c7459b87205bf6dd87f0c13143bd8fe94e4`, no
  runtime state, credentials, databases, recordings, models, backups, generated
  integrations, or donor runtimes;
- CI configuration: PASS; the next frozen SHA is configured for quality,
  deterministic workflows, deterministic permissions, and v1 acceptance.

`READY_TO_FREEZE` means the reviewed mutable tree is eligible for a separately
authorized freeze step. It is not release authorization, a commit, a push, a
tag, publication, or exact-SHA CI evidence. No such action was performed here.

## Final mutable preflight for installed-distribution remediation

Preflight date: 2026-08-27. The mutable tree remains uncommitted at the
historical source candidate SHA
`57862f6777e3dc1966f8f1c07688fde7257e7428`; that SHA is not being relabeled.
The technical packaging remediation is ready for a new candidate: source and
installed integrity paths are distinct, installed evidence is RECORD-bound and
fail-closed, source integrity remains valid, negative integrity tests pass, and
wheel/sdist artifact-only production startup and clean shutdown pass from fresh
environments and unrelated working directories.

Validation on the current mutable tree:

- test-mode `scripts/quality.py`: **PASS** — 1,467 passed, 7 skipped; Ruff,
  strict mypy, and 90% combined coverage passed;
- deterministic workflows: **PASS** — 26 passed;
- deterministic permissions: **PASS** — 72 passed, 1 skipped;
- v1 acceptance: **PASS** — 23 passed;
- artifact smoke: **PASS** — direct wheel and sdist-to-wheel round trip;
- latest pre-freeze wheel: `jarvis-1.0.0-py3-none-any.whl`, 733,995 bytes,
  SHA-256 `60976AEB1F488993DC7E425A4011F3B4E953ECABC1ABA20A8D4D7D7808BFC59A`,
  204 entries;
- latest pre-freeze sdist: `jarvis-1.0.0.tar.gz`, 938,031 bytes, SHA-256
  `281E121EFD8CE74AA48507C195357ECE68D663BD1BABF3F7A22933A87E4A2800`;
- latest sdist round-trip wheel: 733,995 bytes, SHA-256
  `95F0C340ABF2F9D6D26546623C770818BA03BFBC9A471E9571BD29CE0D049A3D`,
  204 entries;
- default local quality: **HOST-LIMITED** — existing Windows Credential
  Manager Win32 error 8 causes 15 fail-closed runtime test failures; no
  plaintext or implicit in-memory production fallback was introduced;
- current mutable source has finalized proprietary `LICENSE.txt`, `EULA.txt`,
  and `PRIVACY_POLICY.txt` documents for private-use/pre-commercial status,
  with PEP 639 custom license metadata. The geographic address is intentionally
  deferred until commercial publication.

The technical result is ready for a private-use technical RC, subject to the
normal freeze and exact-SHA CI process. Commercial publication readiness is
`NOT READY`: final geographic/legal launch details, commercial terms, and any
required consumer disclosures must be completed before commercial launch.

Private-use readiness classification: `READY_TO_FREEZE_PRIVATE_USE`.
Commercial publication status: `NOT READY`.

COMMERCIAL_PUBLICATION_REQUIRES_FINAL_LEGAL_IDENTITY_DETAILS

## Post-audit OpportunityEngine preparation-failure remediation

The independent R4 audit of immutable candidate
`5795cfeedd618d98573bb80f54cb7daecdad7f8d` found that preparation-provider
exceptions still persisted `DETECTED/FAILED`, although proposal and acceptance
correctly rejected the row. That candidate remains permanently `FINAL R4 NO-GO`
and is not modified or relabeled.

The current mutable remediation changes only this lifecycle boundary and its
regressions. Definitive preparation failure now persists as `FAILED/FAILED` for
in-memory and SQLite stores. The central validator rejects inconsistent failed
combinations, and store reload reconciles legacy `DETECTED/FAILED` rows to
`FAILED/FAILED` while retaining evidence and diagnostic errors. Proposal,
acceptance, decline, and restart paths remain fail-closed for failed
opportunities; no automatic retry or authority grant was added.

Focused OpportunityEngine tests pass: 28. The complete quality gate passes:
1,470 tests passed, 7 skipped, 90% coverage, with Ruff and strict mypy passing.
Deterministic workflows pass (26), deterministic permissions pass (72 passed,
1 skipped), v1 acceptance passes (23), and isolated package smoke passes. This
mutable remediation is not committed, pushed, frozen, tagged, published, or
release-authorized. A new preflight and exact-SHA CI audit are required.

The immutable `6cf44a7ced8f55ca194cad05903a48d75f77f400` candidate remains a
historical R4 NO-GO and is not relabeled. This mutable follow-up addresses the
remaining observation-reset defect; a later remediation must produce a new SHA
and repeat the independent release audit.

The follow-up mutable remediation preserves a definitive `FAILED/FAILED`
opportunity when ordinary observation receives the same or additional valid
evidence. It adds no automatic retry or authority. The regression scope includes
in-memory and SQLite observation, restart persistence, acquisition-failure
observation, and the production-composed acceptance path. This tree remains
mutable and is not a release candidate or packaging authorization.

## Historical exact-SHA release-claim reconciliation

The exact historical results remain immutable and are not being relabeled:

* `6cf44a7ced8f55ca194cad05903a48d75f77f400` had exact-SHA CI GREEN and an
  independent R4 NO-GO result of 104 PASS / 2 FAIL / 0 NOT_PROVEN. Criteria 4
  and 100 failed; criterion 86 passed. It was **NOT PACKAGING AUTHORIZED**.
* `ac7fc45f9332f004349e0c7c0868758cc036c8f2` had exact-SHA CI GREEN and an
  independent R4 NO-GO result of 103 PASS / 3 FAIL / 0 NOT_PROVEN. Criteria 4,
  70, and 86 failed; criterion 100 passed. It was **NOT PACKAGING AUTHORIZED**
  and remains an immutable historical candidate.

The current mutable remediation addresses the security-blocked Opportunity
Engine terminality defect and the release-history documentation gap. It remains
uncommitted, unpushed, untagged, unpublished, and not release-authorized until
a separate preflight and exact-SHA audit are completed.

## Security-blocked OpportunityEngine remediation evidence

Starting identity was verified as branch `agent/v1-integration` at immutable
historical SHA `ac7fc45f9332f004349e0c7c0868758cc036c8f2`, with a clean tree
before editing. The remediation is intentionally limited to the Opportunity
Engine, its regressions, and the documentation above.

The resulting contract is:

* `SECURITY_BLOCKED` is canonically paired with `ARCHIVED`;
* passive same/new evidence, restart, expiry, proposal, acceptance, decline,
  and ordinary `prepare()` preserve or reject the block without granting
  reconsideration authority;
* malformed legacy blocked/archive rows reconcile fail closed and persist the
  repaired pair; and
* ordinary `FAILED/FAILED` explicit retry, `UNKNOWN_OUTCOME`, decline cooldown,
  and healthy preparation remain distinct and covered.

Final-tree evidence: `tests/test_capability_opportunities.py` passed 36 tests;
the v1 acceptance suite passed 23 tests (run
`bd52dff3-40f7-4fda-8929-55c74fc10408`); deterministic workflows passed 26
tests (run `347cfbc6-3c1b-4981-9474-96777a68e2dc`); deterministic permissions
passed 72 tests with 1 skip (run `d27fc644-90d7-4b53-80a0-bcf079bc3d5e`);
and package smoke passed. Targeted security/trusted-core tests passed 268
with 1 skip; the one failure is the pre-existing project-anchored startup
check, which is host-limited by `RecoveryAuthorityUnavailable`.

The full `scripts/quality.py` run completed with 1,463 passed, 7 skipped, and
15 pre-existing host-limited runtime/browser/health/persistence failures tied
to unavailable Windows recovery/credential authority. Formatting, Ruff, and
strict mypy passed. Because the required full quality gate is not green on this
host, this mutable remediation is technically resolved but remains
release-blocked pending a host/CI quality run and a fresh independent audit.

SECURITY_BLOCKED_OPPORTUNITY_TERMINALITY: STILL_BLOCKING

## R4N-A2 — Windows Host-Failure Classification

Final result: `HOST_LIMITATION_CONFIRMED`.

The mutable default quality run recorded 1,463 passed, 7 skipped, and 15
failed. Immutable parent `ac7fc45f9332f004349e0c7c0868758cc036c8f2` under the
same host recorded the same 15 failing tests with the same assertion/root-cause
family. No mutable-only failure node IDs were found, and none of the failures
reached or depended on `jarvis/capability_opportunities.py`.

The failures involve Windows Credential Manager availability, trusted
Recovery/LKG authority availability, startup/readiness dependent on unavailable
trusted authority, and health 503 resulting from fail-closed startup state.
The current remediation does not modify CredentialVault, Recovery/LKG, Windows
credential authority, sandbox, or update authority. Production behavior remains
fail closed: there is no plaintext credential fallback and no implicit
recovery-authority fallback.

The repository-supported canonical test mode is `JARVIS_ENVIRONMENT=test`.
Under that mode the mutable tree recorded 1,478 passed, 7 skipped, 0 failed,
90% coverage, Ruff PASS, and strict mypy PASS. The parent recorded 1,475 passed,
7 skipped, 0 failed, and 89% coverage because it predates the remediation tests;
that historical coverage difference is not a current-candidate failure.

The 15 native workstation failures are pre-existing host limitations, not
regressions introduced by the current Opportunity remediation. This
classification does not authorize ignoring any failure in a future hosted
exact-SHA GitHub CI run.

## R4N-B — Final Mutable Preflight Security-Blocked Terminality

Final result: `READY_TO_FREEZE_SECURITY_BLOCK_FIX`.

The verified mutable base is branch `agent/v1-integration`, HEAD
`ac7fc45f9332f004349e0c7c0868758cc036c8f2`, version `1.0.0`. This is a
mutable tree, not a new candidate SHA.

The verified contract is:

* A security-blocked preparation result persists as `ARCHIVED /
  SECURITY_BLOCKED / NONE`.
* Same or new passive evidence, observation after restart, a second SQLite
  restart, and expiry/time passage preserve the block.
* Normal `prepare()`, `decline()`, proposal, and acceptance are rejected before
  provider invocation or state mutation.
* Legacy malformed security-block state reconciles fail closed.
* No trusted security recovery API currently exists; v1 therefore remains fail
  closed.

Ordinary preparation exceptions and explicit failed results persist as
`FAILED / FAILED`; acquisition exceptions do the same. Passive same/new
evidence and restart preserve `FAILED`. Only the explicit application-owned
retry operation for `FAILED` remains available. `SECURITY_BLOCKED` cannot be
retried through ordinary preparation.

`UNKNOWN_OUTCOME` remains distinct and safe, `DECLINED` retains normal cooldown
behavior, healthy opportunities retain normal prepare/proposal/accept/activation
behavior, and semantic self-expansion remains green.

Mutable candidate evidence is:

* Criterion 4 — proactive capability creation: PASS candidate evidence.
* Criterion 70 — durable OpportunityEngine queue: PASS candidate evidence.
* Criterion 86 — release-claim reconciliation accuracy: PASS candidate evidence.
* Criterion 100 — failed-state propagation: PASS regression evidence.

These are not final independent R4 authorization. The next immutable candidate
still requires exact-SHA CI and independent R4N-D.

Release evidence: Opportunity tests 36 passed; deterministic workflows 26
passed; deterministic permissions 72 passed and 1 skipped; v1 acceptance 23
passed; package smoke PASS. Canonical `JARVIS_ENVIRONMENT=test` quality recorded
1,478 passed, 7 skipped, 0 failed, 90% coverage, Ruff PASS, and strict mypy
PASS.

Technical status: READY TO FREEZE A NEW PRIVATE-USE RELEASE CANDIDATE.
Commercial status: NOT READY / NOT AUTHORIZED. No tag, release, or publication
is authorized.

READY_TO_FREEZE_SECURITY_BLOCK_FIX

## R4N-B6 — Controlled Test Runner Remediation

The prior acceptance hang was reproduced as a controlled-runner defect, not a
product regression. Direct `tests/test_v1_acceptance.py` execution passed all
23 tests in 185.44 seconds. The controlled child had omitted the explicit
`JARVIS_ENVIRONMENT=test` selector and used unbounded post-termination pipe
reaping on Windows.

At this historical point, the narrow test-infrastructure remediation propagated
only the exact canonical `JARVIS_ENVIRONMENT=test` value, kept arbitrary
`JARVIS_*` values and representative secrets excluded, used owned-PID Windows
tree cleanup via the System32 `taskkill.exe` path, and bounded communication
reaping. It was an intermediate remediation, later superseded by R4O's native
Job Object ownership path; it is not a claim about the current runner. Normal
pass, failure, launch-error, timeout, cancellation, artifact redaction, and
process-tree behavior remained covered.

Validation used an external Python 3.12.10 environment with locked development
dependencies and `JARVIS_ENVIRONMENT=test`. The runner tests passed 22 tests and the
Opportunity suite passed 36 tests. The
controlled v1 acceptance suite passed independently twice, 23 tests each:
run IDs `9f3e5bf2-f8ec-4d85-a8ac-1ae3b58d02cb` and
`db2440d5-2067-4b1d-b8a2-5bfc5db4e312`. Deterministic workflows passed 26;
deterministic permissions passed 72 with 1 skip.

Canonical quality passed 1,482 tests with 7 skips, 90% coverage, Ruff format
and check PASS, and strict mypy PASS. Package smoke passed PEP 517 wheel,
sdist, round-trip, artifact-only composition, startup/shutdown, and legal
metadata checks. No lingering validation processes were observed after the
successful controlled runs. The native Windows Credential/Recovery host
limitation remains separately classified `HOST_LIMITATION_CONFIRMED`; it was
not weakened or bypassed.

The existing Opportunity security remediation remains unchanged:
`SECURITY_BLOCKED` remains `ARCHIVED / SECURITY_BLOCKED`, ordinary preparation
cannot reopen it, and criterion 100 failed-state propagation remains covered.
The immutable historical parent `ac7fc45f9332f004349e0c7c0868758cc036c8f2`
remains R4 NO-GO and not packaging-authorized.

This remediation is not a freeze, packaging authorization, tag, release, or
publication. A fresh mutable preflight and independent exact-SHA audit are
required before any candidate freeze.

CONTROLLED_TEST_RUNNER: RESOLVED

## R4N-B7 — Final combined mutable prefreeze

The combined mutable remediation was validated from base branch
`agent/v1-integration`, historical parent SHA
`ac7fc45f9332f004349e0c7c0868758cc036c8f2`, version `1.0.0`. The exact mutable
scope is seven files: the OpportunityEngine implementation and tests, the
controlled runner implementation and tests, and the three existing release
documentation files. The current working-tree diff is 574 additions and 6
deletions.

The SECURITY_BLOCKED Opportunity contract remains fail closed and durable:
`ARCHIVED / SECURITY_BLOCKED` remains unchanged across same/new evidence,
two SQLite restarts, observation, expiry, and rejected prepare/decline/proposal/
accept operations. Legacy malformed blocked state reconciles fail closed.
FAILED exception, explicit-failure, acquisition-failure, passive-observation,
restart, and bounded explicit-retry behavior remains covered. UNKNOWN_OUTCOME
remains distinct and non-blindly replayable. Healthy Opportunity flow remains
functional. Criteria 4, 70, and 86 have PASS candidate evidence; criterion 100
has PASS regression evidence.

At that historical point, the Controlled Test Runner narrowly propagated only
exact `JARVIS_ENVIRONMENT=test`, stripped arbitrary environment and secret
values, used owned-PID Windows tree cleanup with fixed System32 `taskkill.exe`,
bounded post-termination reaping, and preserved
PASS/FAILED/CRASHED/MALFORMED/TIMED_OUT/CANCELLED semantics. R4O later replaced
that intermediate cleanup path with native Job Object ownership. Runner tests
passed 22, including the Windows descendant/pipe cleanup regression. No runner
false-green path or unrelated process targeting was introduced.

Validation used the external Python 3.12.10 environment with locked
dependencies and `JARVIS_ENVIRONMENT=test`:

* direct v1 acceptance: 23 passed in 185.44 seconds;
* controlled v1 acceptance #1: 23 passed, run
  `9f3e5bf2-f8ec-4d85-a8ac-1ae3b58d02cb`, status passed;
* controlled v1 acceptance #2: 23 passed, run
  `db2440d5-2067-4b1d-b8a2-5bfc5db4e312`, status passed;
* deterministic workflows: 26 passed, run
  `f73f9f23-a522-495a-bc8e-8ecdb776d645`;
* deterministic permissions: 72 passed and 1 skipped, run
  `8257c285-fd87-4db4-86a8-bb9a14af6900`;
* Opportunity tests: 36 passed;
* full quality: 1,482 passed and 7 skipped, 90% coverage, Ruff format/check
  PASS, strict mypy PASS;
* package smoke: PASS. Wheel SHA-256
  `64824876417B0856D0730C3A0D43D7458E3BECB552368D279008F6DE4CEEB946`, sdist
  SHA-256 `C70F89B47AEAB6EE9EAD844B7C51C22BA653051659001BD28677329EF7501138`,
  and round-trip wheel SHA-256
  `27F1BBFD1F56F30BEF1D8E7637C26BF465B38DD8086DB7EB7E173D94A301D142`.

The earlier local native Credential/Recovery limitation remains
`HOST_LIMITATION_CONFIRMED` and fail closed; it is not waived for future
hosted exact-SHA CI. No Trusted Core, CredentialVault, Recovery/LKG, sandbox,
or Opportunity security semantics were weakened. No donor runtime dependency
was introduced.

This is mutable prefreeze evidence only. It is eligible for R4N-C to freeze a
new immutable private-use release candidate, but is not independent R4
authorization, packaging authorization, a tag, a release, or publication.
Hosted exact-SHA CI and independent audit remain mandatory.

READY_TO_FREEZE_SECURITY_BLOCK_AND_TEST_RUNNER_FIX

## R4O-A/B — exhaustive Candidate 11 remediation and mutable prefreeze

**Run date:** 2026-08-30
**Branch:** `agent/v1-integration`
**Immutable starting candidate:** Candidate 11,
`3bf78458ed5556aaaf72318911874dc85f919be1`
**Version:** `1.0.0`
**Mutable result:** ready for a separately authorized Candidate 12 freeze only.

This is the newest chronological preflight record and supersedes earlier
mutable-preflight readiness markers. It does not rewrite their historical
claims, create Candidate 12, create a Git commit, push, tag, publish, deploy,
or authorize release.

### Candidate 11 historical disposition

Candidate 11 remains immutable `R4_NO_GO`, not packaging-authorized. Its
exact-SHA hosted push CI was genuinely green (run `33276631872`, job
`99164379256`), but the later independent R4 audit found defects in the
trusted release-test infrastructure. The historical matrix is therefore
**98 PASS / 8 FAIL / 0 NOT_PROVEN**. Failed criteria were **4, 17, 25, 26,
30, 70, 86, and 105**; criterion **100** was independently PASS. Historical
green CI does not certify this mutable tree or a future candidate.

### Root causes and remediation

| Area | Candidate 11 root cause | R4O remediation and current evidence |
| --- | --- | --- |
| Opportunity state conflict | Legacy `ARCHIVED / FAILED` data could prefer retryable `FAILED` semantics and reopen a security block. | One central validator/reconciler applies most-restrictive-signal precedence: any `ARCHIVED` or `SECURITY_BLOCKED` marker becomes `ARCHIVED / SECURITY_BLOCKED / NONE`. SQLite/restart and cross-product conflict tests reject ordinary reopening. |
| Unknown preparation outcome | `UNKNOWN_OUTCOME` could be replayed by normal `prepare()`. | It canonicalizes to `ASSESSING / UNKNOWN_OUTCOME / NONE`; ordinary observe, expiry, decline, proposal, acceptance, restart, and prepare preserve/reject it before provider invocation. Only a future explicitly trusted reconciliation contract could alter it. |
| Ordinary failed retry | A safety fix could have accidentally removed valid explicit retry. | `FAILED / FAILED / PREPARE` remains an explicit application-owned retry state, while passive observation remains non-retrying. Criterion 100 regression remains green. |
| Durable opportunity metadata | Untrusted preparation summary/error text could become durable metadata. | Record construction rejects secret-shaped summaries and diagnostic errors before persistence; evidence is reference-oriented rather than a credential/context channel. |
| Controlled test adjudication | Exit-zero malformed output, a completion/cancellation race, and contradictory structured records could produce misleading green evidence. | Pytest text now requires both a session header and terminal framework summary; structured exit-zero output with `FAILED`/`UNKNOWN` records is malformed; cancellation wins same-tick completion; timeout/cancellation/cleanup are non-pass evidence. |
| Process ownership and output | Root exit did not prove owned descendants were gone; cleanup could be ignored; `communicate()` could retain unbounded output; cleanup utility identity depended on mutable environment. | The runner uses the existing trusted Windows Job primitives: suspended launch, assignment before resume, bounded owned ledger/cleanup, no process-name-wide kill, and errors become trusted non-pass evidence. It continuously drains bounded prefix/tail output and resolves the catalogued executable identity without ambient `PATH`, `SystemRoot`, or `WINDIR`. |
| Automation storm | The test asserted an asynchronous queue drain after a fixed delay. | The test uses a bounded semantic condition; the service owns and serializes drain work across dispatch completion/shutdown. The storm passed 50 fresh-process iterations with no failure. |
| Donor provenance | Goose documentation used an obsolete moving-branch observation and an unresolved pin claim. | All authoritative Goose documentation uses `aaif-goose/goose` at immutable `403fcc84c78e5676197219071f4740497fdd4af3`; the official upstream API revalidated that SHA and Apache-2.0 license. It remains reference-only, with no PORT decision or runtime dependency. |
| Release/operator claims | Earlier mutable records and README guidance could mislead an owner about release state or Windows interpreter selection. | `VERSION_LOG.md` supplies factual Candidate 1–11 history; README and development guidance require explicit Python 3.12, explain stale-venv recovery and `.env` non-loading, separate deterministic test mode from normal runtime, and document local Ollama/health/desktop troubleshooting. |

### Adversarial and focused evidence

- Opportunity cross-product and persistence validation: `112` focused
  opportunity/runner/native tests passed; direct opportunity tests include
  `ARCHIVED`/`SECURITY_BLOCKED`, `UNKNOWN_OUTCOME`, restart, expiry, decline,
  proposal, acceptance, and explicit failed-retry cases.
- Runner adversarial coverage rejects fake pytest-looking output, no summary,
  contradictory structured output, nonzero after pass-looking output, timeout,
  cancellation, same-tick cancellation/completion, cleanup error, oversized
  output, hostile environment values, unknown suites, and non-opt-in hardware.
  Native synthetic tests cover root-exit descendants retaining pipes, timeout
  and cancellation cleanup, unrelated-process survival, Windows Job launch
  failure, and pipe setup cleanup.
- Automation queue regression:
  `tests/test_automations.py::test_queue_policy_drains_and_bounds_storm` passed
  **50/50** fresh-process runs. Concurrency, bounds, priority, eventual drain,
  shutdown, and failure-containment regressions remain in the regular suite.
- Donor source/documentation regression: `tests/test_donor_study.py` passed
  **16** tests; no Goose runtime/import/package dependency or copied donor
  source was added.
- Trusted Core/security regression collection: **447 passed, 1 skipped**. The
  skip is the documented test-user inability to create a symlink/junction, not
  a security exclusion or `xfail`.
- Self-expansion composition collection: **156 passed**. It covers generic
  discover/adopt/reuse/build, review/certification, trusted activation,
  verification, removal, restart/reuse, and no product-specific fallback.

### Local release evidence

All current validation used a clean external Python **3.12.10** environment
with locked development dependencies. Deterministic suites and the quality job
used the CI-equivalent `JARVIS_ENVIRONMENT=test` selector. This is a narrowly
scoped synthetic-test seam; it is not a production permission, recovery, or
authority workaround. Artifact package smoke separately starts a production
mode runtime and does not inherit the test selector.

| Check | Result |
| --- | --- |
| README interpreter/dependency check | `py -3.12 --version` and the clean environment both reported 3.12.10; dependency integrity check passed. |
| README focused runner command | `tests/test_system_testing.py`: 36 passed. |
| README health command | Loopback `uvicorn jarvis.api:app` returned `/health` 200 and `/version` `1.0.0` in the disposable test configuration. |
| README desktop command | Offscreen desktop process remained alive through the bounded startup smoke and was then cleanly stopped. This is not a manual UI/hardware claim. |
| Direct v1 acceptance | **23 passed in 317.43s**. |
| Controlled v1 acceptance #1 | **23 passed**, run `4caae60c-2d7f-4915-ba50-c19483864d64`, 420-second bound, no failure evidence. |
| Controlled v1 acceptance #2 | **23 passed**, run `56cddff2-2315-452f-badb-553da150e7f0`, 420-second bound, no failure evidence. |
| Deterministic workflows | **26 passed**, controlled run `699e0cbb-75fc-4143-89ac-7bc32706d217`. |
| Deterministic permissions | **72 passed, 1 skipped**, controlled run `373e5140-8045-440d-bdde-1457f69e2ede`; the skip is the documented local symlink/junction privilege condition. |
| Quality #1 | **1,528 passed, 7 skipped in 443.51s**; Ruff format/check and strict mypy passed; total statement/branch coverage was **90%**. |
| Quality #2 | **1,528 passed, 7 skipped in 394.21s**; Ruff format/check and strict mypy passed; total statement/branch coverage was **90%**. |
| Package smoke | PASS: artifact-only production composition and sdist round-trip both passed. Wheel SHA-256 `374AFA6759EF507BB4E5DA4CC3D6FF25FEAE58B0B125A42A0655E9C2A34F1621`; sdist SHA-256 `7A0A6D3C87CA90E6DB6B9A3937B27DA6CE46DD9978EEC2C72E8681B49992131C`; round-trip wheel SHA-256 `54F7D73F99EC3AE0F24EE0B5109BFFCA06A6B5DDE1762B7DE9C897A0E8E91934`. |

The seven quality skips are existing, explicit host-capability cases: one
adoption identity observation, two reparse/symlink tests, one permission
symlink/junction test, and three opt-in Windows integration/manual hardware
tests. No changed security, runner, OpportunityEngine, workflow, or acceptance
test was skipped, weakened, or ignored.

### CI contract and exact-source discipline

The CI workflow scopes `JARVIS_ENVIRONMENT=test` to the synthetic quality,
deterministic-workflow, deterministic-permission, and v1-acceptance steps. It
does not set that selector for the artifact-only package smoke, which performs
its own production-mode runtime check. This removes the former host-dependent
deterministic-suite ambiguity without globally changing product behavior.

Controlled-run JSON records necessarily carry immutable Git HEAD Candidate 11
while the reviewed files are uncommitted. That field must not be misread as
Candidate 11 validation: it is a source-revision label for a mutable working
tree. A future Candidate 12 commit will be the only valid SHA for hosted CI
and independent audit evidence.

### Candidate 11 blocker reconciliation and R4 mutable preflight

| Historical criterion | Mutable R4O candidate evidence |
| ---: | --- |
| 4 — proactive capability creation | **PASS candidate evidence**: durable safe OpportunityEngine preparation is production-composed; security/unknown failures cannot turn preparation into authority. |
| 17 — UNKNOWN_OUTCOME safety | **PASS candidate evidence**: ordinary opportunity replay is blocked in addition to existing planning/provisioning/replay barriers. |
| 25 — no unresolved release-blocking security finding | **PASS candidate source evidence**: the identified runner false-green/process-cleanup weaknesses now fail closed and the security collection is green. Final independent security assessment remains required. |
| 26 — donor provenance | **PASS candidate evidence**: canonical Goose repository/revision/license/disposition are verified and regression-tested; no donor runtime/source was introduced. |
| 30 — quality suites | **PASS candidate evidence**: two fresh complete quality passes are green at 90% coverage, plus deterministic controlled suites. |
| 70 — OpportunityEngine durable queue | **PASS candidate evidence**: SQLite validation/reconciliation/restart and production-composition tests cover terminal and uncertain states. |
| 86 — release-claim reconciliation accuracy | **PASS candidate evidence**: Candidate 11 is accurately `R4_NO_GO`; `VERSION_LOG.md`, README, and this preflight distinguish historical CI, mutable evidence, and future Candidate 12 evidence. |
| 100 — failed-state propagation | **PASS regression**: `FAILED / FAILED / PREPARE` remains explicit retry-only and does not regress into proposal/acceptance authority. |
| 105 — exact-source CI validity | **SOURCE/INFRASTRUCTURE RESOLVED**: trusted local runner parsing, ownership, cleanup, bounded output, and CI environment scoping are fixed. Exact hosted CI is necessarily deferred to a new immutable SHA. |

The broader 106-criterion R4 preflight was reviewed for every criterion touched
by this remediation: opportunity acquisition/authority/durability, unknown
outcome safety, Trusted Core/security evidence, donor provenance, deterministic
quality, release claims, test infrastructure, documentation, and operator
setup all have local candidate evidence above. No additional source, test, or
documentation blocker was found. The only deferred evidence is inherently
unavailable until a Candidate 12 commit exists:

1. exact-SHA hosted CI for the resulting Candidate 12; and
2. the final independent 106-criterion Sol/max audit of that exact immutable,
   CI-green SHA.

No Candidate 12 currently exists. No commit, staging, push, amend, merge,
rebase, tag, release, publication, or deployment occurred in this run.
The mutable diff is subject to the final file-by-file audit immediately
following this documentation update, before any separately authorized freeze
operation.

READY_TO_FREEZE_R4O_ALL_KNOWN_BLOCKERS

## R4P — Candidate 12 Windows Job accounting remediation

**Run date:** 2026-08-30

**Branch:** `agent/v1-integration`

**Immutable base:** Candidate 12, `b55f8a4dd1364e0c588df0d63de1cd0025b18eba`
**Candidate 12 status:** `RC_CI_FAILED`, immutable and not packaging-authorized.

Candidate 12's exact hosted push CI failed in run `33339183982`, job
`99331570440`: quality, deterministic workflows, and deterministic permissions
passed; v1 acceptance failed with `process_cleanup_failed`; package smoke was
skipped. Candidate 12 did not reach independent R4 audit and must not be amended.

### Confirmed root cause and replacement proof

The trusted Windows Job wrapper used `WaitForSingleObject(job_handle, timeout)`
as its `wait_for_empty()` predicate. A real disposable native diagnostic created
an owned Job, launched a harmless Python child suspended into it, and observed
that after child exit `QueryInformationJobObject` reported
`ActiveProcesses == 0` while a zero-timeout wait on the Job handle continued to
return `WAIT_TIMEOUT` (`258`). The Job handle signal is therefore not accepted
as generic Job-empty evidence.

`_WindowsJob` now binds `QueryInformationJobObject` and the Win32
`JOBOBJECT_BASIC_ACCOUNTING_INFORMATION` layout, exposes a fail-closed
`active_process_count()`, and implements bounded polling of
`ActiveProcesses == 0`. Query failure is never converted into zero members.
Normal root completion first observes bounded accounting convergence; timeout
and cancellation invoke owned termination and then require the same accounting
proof. Job close remains the kill-on-close backstop, serialized against native
accounting queries. The exact descendant ledger remains a separate backstop for
observed breakaway/nesting edges; it is not replaced by accounting.

### Native Windows and release evidence

- Focused sandbox/runner regression: **64 passed**. It includes a real native
  Job normal-exit accounting stress loop of **25/25**, a real owned descendant
  cleanup, and a real `SubprocessTestAdapter` normal-exit regression with no
  cleanup error.
- Controlled v1 acceptance #1: **23 passed**, run
  `0f9bbb07-450b-4c05-9c11-a0cefdd5d98c`, no failure evidence.
- Controlled v1 acceptance #2: **23 passed**, run
  `50c6fffd-0033-47b8-ba01-1cd0921c330c`, no failure evidence.
- Quality #1: **1,533 passed, 7 skipped**, **90%** coverage; Ruff and strict
  mypy passed.
- Quality #2: **1,533 passed, 7 skipped**, **90%** coverage; Ruff and strict
  mypy passed.
- Deterministic workflows: **26 passed**, run
  `c8978874-5a54-4ded-be05-56e99d9bdf40`.
- Deterministic permissions: **72 passed, 1 skipped**, run
  `9738fca7-9b0e-416f-ae48-642bea7f97f0`.
- Focused Opportunity, automation, donor-provenance, sandbox/Windows, and
  Trusted Core collection: **252 passed**.
- Package smoke: PASS. Wheel SHA-256
  `B9ED9E263E122161534AEF301962FB594491DBBF907FC2AB5A59D39B013AA09D`,
  sdist SHA-256
  `CF05FEAFF07637A531934B32143DDF839DBC016F42C896D879712669B7316655`, and
  round-trip wheel SHA-256
  `3428F0C2115AC07B946982DF695E0C44468FFAB452F1B4E9650B33BE2CD7A814`.

The seven quality skips are pre-existing documented host-capability/manual
conditions; the new native Job accounting tests execute on Windows and are not
skipped. No donor runtime dependency, product-specific integration, security
exclusion, or cleanup-policy weakening was introduced. This remains a mutable
Candidate 13 prefreeze only. The only deferred evidence is Candidate 13 freeze,
exact-SHA hosted CI, and a final independent Candidate 13 R4 audit.

READY_TO_FREEZE_R4P_JOB_EMPTY_FIX
